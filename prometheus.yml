global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'ml-service'
    static_configs:
      - targets: ['host.docker.internal:8080']  # or 'localhost:8080' if it works
        # If your python service runs on host